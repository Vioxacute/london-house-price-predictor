services:
  - type: web
    name: london-house-price-predictor
    runtime: python
    buildCommand: "pip install -r requirements.txt && python train.py"
    startCommand: "streamlit run app.py --server.port $PORT --server.address 0.0.0.0"